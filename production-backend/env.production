# Production Environment Configuration
NODE_ENV=production
PORT=8080

# Database Configuration (if needed in future)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=dataroom

# API Configuration
API_PORT=8080
API_HOST=0.0.0.0

# Mock Mode (for production demo)
MOCK_ENABLED=true

# CORS Configuration
ALLOWED_ORIGINS=https://vlzen.com,http://localhost:5173 